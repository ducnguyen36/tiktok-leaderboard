# Disable the React error overlay in development mode
# This is especially useful for TV browsers like LG WebOS that may have compatibility issues
FAST_REFRESH=false

# Disable WebSocket for Hot Module Replacement (can cause "Script error" on TV browsers)
WDS_SOCKET_PORT=0
